_schema-version: "3.2"
ID: masterdetail
description: A Fiori application.
version: 0.0.1
modules:
- name: masterdetail-dest-content
  type: com.sap.application.content
  requires:
  - name: masterdetail-dest-srv
    parameters:
      content-target: true
  - name: masterdetail-repo-host
    parameters:
      service-key:
        name: masterdetail-repo-host-key
  - name: masterdetail-uaa
    parameters:
      service-key:
        name: masterdetail-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: masterdetail_repo_host
          ServiceInstanceName: masterdetail-html5-srv
          ServiceKeyName: masterdetail-repo-host-key
          sap.cloud.service: masterdetail
        - Authentication: OAuth2UserTokenExchange
          Name: masterdetail_uaa
          ServiceInstanceName: masterdetail-xsuaa-srv
          ServiceKeyName: masterdetail-uaa-key
          sap.cloud.service: masterdetail
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: masterdetail-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: masterdetail-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - masterdetail.zip
      name: masterdetail
      target-path: resources/
- name: masterdetail
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: masterdetail-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: masterdetail-dest-srv
    service-plan: lite
- name: masterdetail-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: masterdetail-xsuaa-srv
    service-plan: application
- name: masterdetail-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: masterdetail-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
